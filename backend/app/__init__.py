# ---------------------------------------------------------------------------
# app/__init__.py
# Marks "app" as a Python package. This is the root package for the entire
# SkyGuard AI backend — every module below (api, core, db, models, schemas,
# services, config) lives inside this package.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# app/api/v1/endpoints/__init__.py
#
# PURPOSE OF THIS FOLDER:
# One module per resource/feature (e.g. health.py, and in the future
# users.py, flights.py, alerts.py, ...). Each module defines an
# APIRouter with its own endpoint functions, which is then included by
# app/api/v1/router.py.
# ---------------------------------------------------------------------------
